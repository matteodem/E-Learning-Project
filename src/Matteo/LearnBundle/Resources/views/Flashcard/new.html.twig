<h1>Flashcard creation</h1>

<form action="{{ path('learn_flashcard_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('learn_flashcard') }}">
            Back to the list
        </a>
    </li>
</ul>
